# A simple api with only one path route 

The only endpoint of the API `@app.get('/')` retruns a dictionary contain of a message 'Hello World!'.

Note: the hello_world function is also called *route handler*.

